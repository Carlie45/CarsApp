<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context=".activities.LinearLayoutActivity"
    tools:showIn="@layout/activity_linear_layout">

    <RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:id="@+id/appToolbar"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:background="@color/colorDarkBlue">

        <Button
            android:id="@+id/buttonBack"
            style="@style/ToolbarBackButton"/>

        <TextView
            android:id="@+id/textViewLoggedInEmail"
            style="@style/ToolbarEmailTextView" />

    </RelativeLayout>

    <Button
        android:id="@+id/buttonListCarsAlternatively"
        style="@style/FullWidthButton"
        android:text="@string/list_cars_alternatively"
        android:layout_below="@id/appToolbar"/>

    <ScrollView
        android:id="@+id/scrollViewListCars"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/buttonListCarsAlternatively"
        android:fillViewport="false">

        <LinearLayout
            android:id="@+id/linearLayoutCarsDetails"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical"/>

    </ScrollView>

</RelativeLayout>